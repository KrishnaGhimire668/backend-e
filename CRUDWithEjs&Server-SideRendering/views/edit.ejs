<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Edit User</title>
  </head>
  <body>
    <div class="w-full h-screen bg-zinc-900 p-10 text-white">
      <div class="nav mb-10">
        <a class="text-blue-500" href="/read">Back to Users</a>
      </div>
      <h1 class="text-3xl tracking-tighter mb-5">Edit User</h1>

      <form
        class="flex flex-col gap-4 w-1/3"
        action="/update/<%= user._id %>"
        method="post"
      >
        <input
          class="px-5 py-2 border-2 border-zinc-800 rounded-lg outline-none bg-transparent"
          type="text"
          name="name"
          placeholder="Name"
          value="<%= user.name %>"
        />
        <input
          class="px-5 py-2 border-2 border-zinc-800 rounded-lg outline-none bg-transparent"
          type="email"
          name="email"
          placeholder="Email"
          value="<%= user.email %>"
        />
        <input
          class="px-5 py-2 border-2 border-zinc-800 rounded-lg outline-none bg-transparent"
          type="text"
          name="image"
          placeholder="Image URL"
          value="<%= user.image %>"
        />

        <input
          class="px-5 py-2 bg-blue-500 hover:bg-blue-600 cursor-pointer rounded-lg transition-all"
          type="submit"
          value="Update User"
        />
      </form>
    </div>
  </body>
</html>
