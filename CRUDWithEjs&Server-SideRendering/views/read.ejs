<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>All Users</title>
  </head>
  <body>
    <div class="w-full h-full min-h-screen bg-zinc-900 p-10 text-white">
      <a class="text-blue-500 hover:text-blue-400" href="/">back to home</a>
      <h1 class="text-3xl mt-5 tracking-tighter mb-3">All Users</h1>
      
      <div class="users flex gap-7 flex-wrap">
        
        <% if(users.length > 0) { %>
          <% users.forEach(function(user){ %>
            <div class="user w-72 p-4 bg-zinc-800 rounded-lg">
              <div class="w-full h-52 rounded-lg bg-zinc-700 overflow-hidden">
                <img class="w-full h-full object-cover" src="<%= user.image %>" alt="User Image">
              </div>
              <h3 class="text-xl mt-3 tracking-tighter"><%= user.name %></h3>
              <h5 class="text-zinc-500 font-medium"><%= user.email %></h5>
              
              <div class="flex gap-3 mt-8">
                <a class="text-zinc-200 hover:text-white" href="/edit/<%= user._id %>">Edit User</a>
                <a class="text-red-600 hover:text-red-400" href="/delete/<%= user._id %>">Delete User</a>
              </div>
            </div>
          <% }) %>
        <% } else { %>
          <h3 class="text-zinc-500">No users found in database.</h3>
        <% } %>

      </div>
    </div>
  </body>
</html>