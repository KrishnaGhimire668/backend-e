<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white">
    <div class="w-full min-h-screen p-10">
        <div class="w-full flex justify-end">
            <a class="bg-zinc-800 rounded-md px-3 py-2 text-sm mb-5 inline-block hover:bg-zinc-700 transition-colors" href="/profile">Back to Profile</a>
        </div>

        <h3 class="text-3xl mb-5 font-semibold">Edit your post</h3>

        <form method="post" action="/update/<%= post._id %>">
            <textarea
                name="content"
                class="w-full md:w-1/3 block p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md focus:border-blue-500 min-h-[150px]"
            ><%= post.content %></textarea>
            
            <input
                class="px-3 w-40 py-2 bg-blue-500 hover:bg-blue-600 text-sm block rounded-md mt-4 cursor-pointer transition-colors"
                type="submit"
                value="Update Post"
            />
        </form>
    </div>
</body>
</html>