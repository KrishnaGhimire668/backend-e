<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-zinc-900 text-white">
    <div class="w-full min-h-screen p-10">
      <div class="w-full flex justify-end">
        <a
          class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block hover:bg-red-600 transition-colors"
          href="/logout"
          >Logout</a
        >
      </div>

      <h3 class="text-3xl">
        <span class="font-light">Hello</span>, <%= user.name %> Welcome!
      </h3>
      <h5 class="mb-5 text-zinc-400">What's on your mind?</h5>

      <form method="post" action="/post">
        <textarea
          placeholder="Write your thoughts..."
          class="w-full md:w-1/3 block p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md focus:border-blue-500"
          name="content"
        ></textarea>
        <input
          class="px-3 w-40 py-2 bg-blue-500 hover:bg-blue-600 text-sm block rounded-md mt-2 cursor-pointer transition-colors"
          type="submit"
          value="Create new post"
        />
      </form>

      <div class="posts mt-20">
        <h3 class="text-zinc-400 font-semibold mb-5">Your Posts</h3>
        <div class="postcontainer flex flex-wrap gap-4">
          <% user.posts.reverse().forEach(function(post){ %>
          <div
            class="post w-full md:w-[30%] p-4 rounded-md border-[1px] border-zinc-700 bg-zinc-800/30"
          >
            <h4 class="text-blue-500 mb-2 font-medium">
              @<%= user.username %>
            </h4>
            <p class="text-sm tracking-tight"><%= post.content %></p>

            <small class="mt-3 inline-block text-zinc-500">
              <%= post.likes ? post.likes.length : 0 %> likes
            </small>

            <div class="btns flex gap-4 mt-4">
              <a
                class="text-blue-500 hover:text-blue-400"
                href="/like/<%= post._id %>">
                <%= post.likes.indexOf(user._id) === -1 ?
                "Like" : "UnLike" %>
              </a>
              <a
                class="text-zinc-400 hover:text-white"
                href="/edit/<%= post._id %>"
                >Edit</a
              >
            </div>
          </div>
          <% }) %>
        </div>
      </div>
    </div>
  </body>
</html>
