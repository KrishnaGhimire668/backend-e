<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-zinc-900 text-white">
    <div class="w-full min-h-screen p-10">
      
      <div class="w-full flex justify-end mb-10">
        <a class="bg-red-500 rounded-md px-3 py-2 text-sm hover:bg-red-600 transition-colors" href="/logout">Logout</a>
      </div>

      <div class="flex items-center gap-4 mb-3">
        <div class="w-12 h-12  rounded-md flex-shrink-0">
          <img class="w-full h-full object-cover" src="./images/uploads/<%= user.profilepic %>" alt="">
        </div>
        
        <h3 class="text-3xl tracking-tight">
          <span class="font-light">Hello</span>, <%= user.name %> Welcome!
        </h3>
      </div>

      <h5 class="text-zinc-400 mb-6">What's on your mind?</h5>

      <form method="post" action="/post">
        <textarea
          placeholder="Write your thoughts..."
          class="w-full md:w-1/3 block p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md focus:border-blue-500"
          name="content"
        ></textarea>
        <input
          class="px-5 py-2 bg-blue-500 hover:bg-blue-600 text-sm block rounded-md mt-4 cursor-pointer transition-colors font-medium"
          type="submit"
          value="Create new post"
        />
      </form>

      <div class="posts mt-16">
        <h3 class="text-zinc-400 font-semibold mb-6">Your Posts</h3>
        
        <div class="postcontainer flex flex-wrap gap-6">
          <% user.posts.reverse().forEach(function(post){ %>
          <div class="post w-full md:w-[31%] p-5 rounded-lg border border-zinc-800 bg-zinc-800/20 hover:bg-zinc-800/40 transition-all">
            <h4 class="text-blue-500 mb-2 font-medium">@<%= user.username %></h4>
            <p class="text-zinc-200 leading-relaxed"><%= post.content %></p>

            <div class="flex items-center gap-2 mt-4">
               <small class="text-zinc-500">
                <%= post.likes ? post.likes.length : 0 %> likes
              </small>
            </div>

            <div class="btns flex gap-6 mt-4 border-t border-zinc-800 pt-4">
              <a class="text-blue-500 hover:text-blue-400 text-sm font-medium" href="/like/<%= post._id %>">
                <%= (post.likes && post.likes.indexOf(user._id) !== -1) ? "Unlike" : "Like" %>
              </a>
              <a class="text-zinc-500 hover:text-white text-sm font-medium" href="/edit/<%= post._id %>">Edit</a>
            </div>
          </div>
          <% }) %>
        </div>
      </div>
    </div>
  </body>
</html>